# Design(设计):<Plan/功能名>

> 设计层(HOW)。**引用既有家、不复制**:契约→`contracts/openapi/X.yaml`;跨切/争议决策→`ADR-xxx`;探查事实→`spikes/…`。
> 对应 spec.md 的需求;过 DoR 单门后才进 tasks(superpowers:writing-plans)。

## 架构
- **模块分层 / 隔离边界**:<新增/改动的模块、物理隔离、依赖方向(不破分层)>
- 与既有架构/宪法的一致性:<隔离 / can() 唯一出入口 / 契约先行>

## 核心流程 / 协议
- <关键流程的步骤序列;状态流转;时序要点>

## 数据模型
- **<实体/DataClass>**:字段 / 类型 / **不变量**;标识(不透明 ID / 隔离编码);持久化形态(存哪 / 一致性 / 无 PG 取舍)
- **状态机**(如有):<状态集 + 合法转移>

## 授权与安全
- **隔离 / can()**:对哪些资源、按什么维度(企业/组)过滤;deny 行为
- **威胁与红线**:<伪造/越权/注入等;本 plan 的安全红线 = 必过的负向测试>

## 非功能(NFR)
- **并发 / 可靠性**:<竞态、锁、失败/孤儿处理>
- **安全**:<secret 管理 / CSRF / 会话吊销窗口…>
- **性能 / 规模**:<阈值;超阈值的显式登记 vN+>
- **dev/prod parity + 部署拓扑**:<本地=云上同依赖同功能,仅数据量/部署不同>

## 依赖与引用
- **契约**:`contracts/openapi/<X>.yaml`(由 Task <n> 产出 / 已存在)
- **决策(ADR)**:`ADR-xxx` <一句话决策>
- **探查事实**:`spikes/<…>/probe.md` <实测结论 / 决策规则>

## 技术选型理由
- <技术 A>:为什么选(对比否决方案)

## ★ DoR 自检(进 tasks 前过门,逐项三态:已决定 / 显式推迟+理由 / 待探查+探针+决策规则)
- [ ] 1 范围与出口:<状态>
- [ ] 2 接口契约(对外契约已过"第一个消费者":UI 低保真原型 / 程序 SDK·样例;纯内部豁免):<状态>
- [ ] 3 数据模型:<状态>
- [ ] 4 外部依赖事实(实测成文):<状态 / spike 链接>
- [ ] 5 行为·边界·并发·威胁:<状态>
- [ ] 6 NFR(安全/隔离/性能/parity/拓扑):<状态>
- [ ] 7 验收与测试策略(runbook):<状态>
- [ ] 8 关键决策留痕(ADR):<状态>
