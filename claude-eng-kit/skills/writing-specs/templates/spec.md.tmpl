# Spec(需求):<Plan/功能名>

> 需求层(WHAT / WHY)。**禁**技术栈 / 契约 schema / 实现细节(那些归 design.md / tasks)。
> 业务可读。未决处用 `[NEEDS CLARIFICATION: 具体问题]` 标记,不要静默猜。

## 关联出口 / Sprint
- 出口:<关哪个 sprint 出口 / 验收标准>
- Sprint:<所属 sprint>;ADR(若有):<ADR-xxx>

## Goal & 价值
- <一句话目标>
- <对用户/业务的价值;成功长什么样>

## 范围
- **In(本 plan 做)**:<…>
- **Out(本 plan 不做)**:<…>
- **推迟(vN+ / backlog,带理由)**:<…>

## 用户场景 & 验收(可证伪)
- **场景 1**:作为 <角色>,我要 <做什么>,以便 <价值>
  - **AC-1** Given <前置> When <动作> Then <可观察结果>
  - **AC-2** Given … When … Then …
- **场景 2**:…
- **边界 / 异常场景**:<空 / 超大 / 越权 / 冲突 / 失败时的期望>

## 功能需求(可测)
- **FR-001**:系统 MUST <可验证的行为>
- **FR-002**:系统 MUST <…>
- **FR-003**:系统 MUST NOT <禁止的行为 / 隔离不变式>

## 关键实体(概念级,非 schema)
- **<实体 A>**:是什么、关键属性(概念)、与谁的关系
- **<实体 B>**:…

## 未决(进 DoR 前须清零或转探针/推迟)
- `[NEEDS CLARIFICATION: …]`
